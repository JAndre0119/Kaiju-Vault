# Kaiju-Vault

CPSC 449 semester project. Currently a checkpoint proof-of-concept: a minimal React + Vite frontend that searches movie titles via the TMDB API.

## Setup

1. `npm install`
2. Copy `.env.example` to `.env.local` and set `VITE_TMDB_API_KEY` to your TMDB API key.
3. `npm run dev`
